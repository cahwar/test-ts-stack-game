Admin System

- Service: Check Access, Register Commands, Handle Command Requests ✅

- Controller: Fetch access status, Fetch commands (names), create commands (names) atom to pass it to react component, send request method ✅

- UI: If has access then mount HUD button for testing commands, admin panel page with button for each command registered that sends request on click
